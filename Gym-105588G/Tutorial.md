这个题解的核心 trick 是**利用二进制最低位奇偶性分析**来控制最大公因数（GCD）的奇偶性，从而在有限步数内将一个数归零。具体来说：

- 若两数均为偶数，则GCD必为偶数，可同时除以2（不影响答案方向）；
- 若某数为奇数，则GCD为奇数，将该数减去GCD后会变为偶数。

这样每两步至少能将一个偶数除以2，因此至多约24步就能将其中一个数（如a）除以4096，使其减小到1以内，再一步归零。从而证明最优解不超过26步，之后可通过有限步数的搜索（如枚举操作序列）在可行时间内求解。